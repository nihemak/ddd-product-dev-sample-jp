/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // React 19では不要になる可能性があるため、一旦コメントアウト
  // experimental: {
  //   appDir: true, // Next.js 13以降はデフォルトで有効
  // },
};

export default nextConfig;
